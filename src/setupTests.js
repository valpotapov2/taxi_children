// Файл ожидается конфигурацией jest в package.json (setupFilesAfterEnv),
// но в репозитории его не было — без него не запускается ни один тест.
// Общей подготовки тестам пока не требуется.
export {}
