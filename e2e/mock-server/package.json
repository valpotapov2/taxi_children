{
  "name": "nanny-breaks-mock",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Мок-сервер API перерывов по контракту ТЗ-001 (docs/ТЗ-001-контракт-API-перерывов.md)",
  "scripts": {
    "start": "node src/server.ts",
    "test": "node --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/multer": "^2.0.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  }
}
