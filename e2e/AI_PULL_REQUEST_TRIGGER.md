# CI pull_request trigger diagnostic

This file exists only to trigger a new commit on the existing PR so GitHub Actions can be observed for the `pull_request` event. It does not affect application code or E2E tests.
